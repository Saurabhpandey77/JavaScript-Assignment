//Declare a varibale and assign string value to it and change all the string characters to capital letters using toUpperCase() method.

let text = "saurabh pandey";
let result = text.toUpperCase(text);
console.log(result)